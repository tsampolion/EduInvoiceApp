## Build System

This project uses a centralized Gradle Version Catalog. All dependency and plugin versions are defined in `gradle/libs.versions.toml`. Modules reference dependencies using `libs.*` aliases and align Compose and Firebase using BoMs.


